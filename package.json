{
  "name": "hunthub",
  "version": "1.0.0",
  "private": true,
  "description": "HuntHub - Treasure hunt platform",
  "workspaces": [
    "apps/backend/*",
    "apps/frontend/*",
    "packages/*"
  ],
  "scripts": {
    "build:shared": "npm run build --workspace=@hunthub/shared",
    "build:api": "npm run build --workspace=@hunthub/api",
    "build:backend": "npm run build:api",
    "build:editor": "npm run build --workspace=@hunthub/editor",
    "build:frontend": "npm run build:editor",
    "build:all": "npm run build --workspaces --if-present",
    "dev:api": "npm run dev --workspace=@hunthub/api",
    "dev:backend": "npm run dev:api",
    "dev:editor": "npm run dev --workspace=@hunthub/editor",
    "dev:frontend": "npm run dev:editor",
    "generate": "npm run generate --workspace=@hunthub/shared",
    "test:api": "npm run test --workspace=@hunthub/api",
    "test:api:verbose": "npm run test:verbose --workspace=@hunthub/api",
    "test:api:watch": "npm run test:watch --workspace=@hunthub/api",
    "test:api:coverage": "npm run test:coverage --workspace=@hunthub/api",
    "test:api:debug": "npm run test:debug --workspace=@hunthub/api",
    "lint": "eslint . --ext .ts",
    "format": "prettier --write \"**/*.{ts,json,md}\"",
    "type-check": "tsc --noEmit --workspaces",
    "type-check:editor": "npm run type-check --workspace=@hunthub/editor"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.45.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-prettier": "^5.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.3.3"
  }
}
