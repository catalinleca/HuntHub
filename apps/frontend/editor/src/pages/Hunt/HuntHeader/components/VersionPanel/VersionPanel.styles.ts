import { styled } from '@mui/material/styles';
import { List } from '@mui/material';

export const VersionList = styled(List)`
  max-height: 200px;
  overflow-y: auto;
  padding-top: 0;
  padding-bottom: 0;
`;
