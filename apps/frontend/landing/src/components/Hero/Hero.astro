---
import './Hero.styles.css';
import PromptForm from '../PromptForm/PromptForm.astro';
import { EDITOR_URL } from '../../config/urls';
---

<section class="hero-gradient min-h-screen pt-32 px-6 pb-24 flex items-center">
  <div class="w-full max-w-5xl mx-auto text-center">
    <h1
      class="font-display text-5xl lg:text-6xl font-semibold text-foreground mb-6 max-w-3xl mx-auto leading-tight tracking-tight"
    >
      Create a treasure hunt <span class="text-primary">made just for them</span>
    </h1>

    <p class="text-lg text-foreground-muted mb-10 max-w-2xl mx-auto leading-relaxed">
      HedgeHunt turns your idea into a playful adventure with clues, locations, and challenges. Personalize it, share a
      link, and watch them play.
    </p>

    <div class="flex flex-col sm:flex-row items-center justify-center gap-3 mb-8">
      <a href={EDITOR_URL} class="cta-button px-6 py-3 rounded-full font-medium whitespace-nowrap"> Create a hunt </a>
      <a href="#how-it-works" class="cta-button-secondary px-6 py-3 rounded-full whitespace-nowrap">
        See how it works
      </a>
    </div>

    <p class="text-sm text-foreground-muted mb-6 max-w-2xl mx-auto leading-relaxed">
      Perfect for birthdays, proposals, friend groups, classrooms, and community events.
    </p>

    <PromptForm />
  </div>
</section>
